module.exports.Router = (function(routerware,r,utility,debug){

	var util = utility,
		url = require('url'),
		domain = require('domain'),
		url = require('url'),
		r = r,
		Router = function(server){
			this.server = server;
		};

	return Router;

});